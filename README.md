This project is a React-based Training Management System for managing subjects and courses.
The application is structured around a single App component that logically separates Subjects and Courses modules.
Component Structure:
   React (Functional Components + Hooks),
   CSS (Flexbox Layout),
   Browser localStorage
